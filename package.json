{
  "name": "gitgraph-x-2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm  run build && cd packages/main && npm start",
    "build": " cd packages/common && npm run build && cd ../frontend && npm run build && cd ../main && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.9.3"
  }
}
